<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Speed Checker</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      h1 {
        color: #333;
        font-size: 1.5em;
        margin-bottom: 20px;
        text-align: center;
      }

      form {
        background-color: #fff;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        max-width: 400px;
        margin: 0 auto;
        width: 100%;
      }

      input[type="url"] {
        width: calc(100% - 22px);
        padding: 10px;
        margin-bottom: 15px;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 1em;
      }

      button {
        width: 100%;
        padding: 10px;
        background-color: #28a745;
        color: #fff;
        border: none;
        border-radius: 4px;
        font-size: 1em;
        cursor: pointer;
      }

      button:hover {
        background-color: #218838;
      }
    </style>
</head>
<body>
        <form action="/check" method="POST">
            <h1>Saytlarning  samaradorligi va  tezligini tekshiring!</h1>
            <input type="url" name="url" placeholder="Har qanday sayt domenini kiriting ...." required>
            <button type="submit">Tekshirish</button>
        </form>
</body>

<script>
    const form = document.querySelector('form')
    const checkBtn = form.querySelector('button')
    form.addEventListener('submit', (e) => {
      checkBtn.textContent = "Tekshirilayapdi..."
    })
</script>
</html>
